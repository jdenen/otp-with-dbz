defmodule DbzTest do
  use ExUnit.Case

  describe "main_character/0" do
    test "returns a living Goku process"
  end

  describe "new_character/3" do
    test "creates a new character"
    test "creates powerful characters who cannot be permanently killed"
    test "creates weak characters who can be permanently killed"
  end
end
